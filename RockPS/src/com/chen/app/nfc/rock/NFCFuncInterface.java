package com.chen.app.nfc.rock;

/**
 *   This interface is used for nfc function calls
 *   normally we will use Android Beam function call
 *   in case we have a ealier Android version
 *   we use the old nfc calls
 * 
 *  @author chen
 *
 */
public interface NFCFuncInterface {

}
